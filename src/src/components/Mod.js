const root = "../../../projects"

import Icon from 'Icon'

const Mod = (props) => {
    return(
        <div>
            <Font value={this.props.modName} /> <Icon/>
        </div>
    )
}

export default Mod;